import { Route } from '@ant-design/pro-layout/es/typing';

const routes: Route[] = [
    { path: '/apps', name: '应用中心' },
    { path: '/userinfo', name: '账号设置' },
    { path: '/organize', name: '组织架构' },
];

export default routes;
